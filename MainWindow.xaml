<Window x:Class="Disassembler.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PE Disassembler" Height="350" Width="600" Background="#1E1E1E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Выберите .exe файл:" 
                   Foreground="White" FontSize="14" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="FilePathBox" Width="400" Margin="0,0,10,0"/>
            <Button Content="Обзор..." Width="80" Click="Browse_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="Проверить" Width="120" Height="30" 
                    Click="Check_Click" Margin="0,0,10,0"/>
            <Button Content="Дизассемблировать" Width="120" Height="30" 
                    Click="Disassembler_Click" x:Name="DisassemblerBtn" IsEnabled="False"/>
        </StackPanel>

        <TextBox Grid.Row="3" x:Name="ResultText" 
                 Foreground="LightGreen" Background="#2D2D30" FontSize="12" 
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 IsReadOnly="True" TextWrapping="Wrap" BorderThickness="1" BorderBrush="#555555"/>
    </Grid>
</Window>